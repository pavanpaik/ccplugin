List all Claude Code plugins currently managed by ccplugin.

Usage: /ccplugin-list

Run the following command and show the output verbatim:

```bash
ccplugin list
```

The output shows each plugin's key, scope, version, and source URL.
