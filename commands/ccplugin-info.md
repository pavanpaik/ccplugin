Show detailed information about a specific ccplugin-managed plugin.

Usage: /ccplugin-info <plugin-name>

Run the following command and show the output verbatim:

```bash
ccplugin info $ARGUMENTS
```

The output includes version, scope, install path, source URL, git commit SHA, and detected components (skills, commands, agents, hooks, MCP servers).
