Uninstall a Claude Code plugin managed by ccplugin.

Usage: /ccplugin-uninstall <plugin-name> [--scope user|project|local]

Run the following command and show the output verbatim:

```bash
ccplugin uninstall $ARGUMENTS
```

After uninstalling, remind the user to restart Claude Code for the change to take effect.
