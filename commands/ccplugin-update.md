Update a ccplugin-managed Claude Code plugin to the latest version from its original source.

Usage: /ccplugin-update <plugin-name>

Run the following command and show the output verbatim:

```bash
ccplugin update $ARGUMENTS
```

This re-fetches from the original Git URL or re-copies from the original local path recorded at install time.
